/**
 * Corese adapter layer.
 *
 * <p>This package is the only one allowed to depend on corese-core types. GUI code must never
 * import or expose corese-core classes; it should interact with these services via stable DTOs.
 */
package fr.inria.corese.gui.core.adapter;
